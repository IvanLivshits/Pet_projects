'use strict';
/*
const ivan = {
    firstName: 'Ivan',
    lastName: 'Livshits',
    birthYear: 1999,
    job: 'student',
    friends: ['Mariia', 'Egor', 'Anton'],
    haveDriversLicense: true,

    calcAge: function () {
        this.age = 2022 - this.birthYear;
        return this.age;
    },

    getSummary: function () {
        this.summary = `${this.firstName} is a ${this.age}-year old ${this.job} and he ${this.haveDriversLicense ? `has` : `doesn't have`} a driver's license.`;
        return this.summary;
    }
};

ivan.calcAge(), ivan.getSummary();

console.log(ivan);


let num = 1;
let ender = 3;
while (num != ender) {
    console.log(`U rolled ${num}`);
    num = Math.trunc(Math.random() * 6) + 1;
    if (num === ender) console.log(`Uhuuuu u can won! U rolled ${num}`)
}
*/